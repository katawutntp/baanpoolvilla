# 📋 คู่มือแก้ไข UX/UI — BaanPoolVilla Website

> สรุปจุดที่แก้ไขได้เอง โดยไม่ต้องเขียนโค้ดเพิ่ม แค่เปลี่ยนข้อความ/URL/ตัวเลข

---

## 🔴 ต้องแก้ก่อน Launch (ข้อมูล placeholder)

### 1. เบอร์โทรศัพท์ (3 จุด)

| ไฟล์ | บรรทัด | ค่าปัจจุบัน |
|------|--------|-------------|
| `src/components/Footer.js` | ~L82 | `'062-xxx-xxxx'` |
| `src/app/contact/page.js` | ~L38 | `'062-xxx-xxxx'` + `tel:+66123456789` |
| `src/components/BookingForm.js` | ~L304 | `tel:+66123456789` |

### 2. อีเมล (2 จุด)

| ไฟล์ | บรรทัด | ค่าปัจจุบัน |
|------|--------|-------------|
| `src/components/Footer.js` | ~L88 | `'info@baanpoolvilla.com'` |
| `src/app/contact/page.js` | ~L44 | `'info@baanpoolvilla.com'` |

### 3. ลิงก์ Social Media (5 จุด — ปัจจุบันเป็น `#`)

| ไฟล์ | บรรทัด | อะไร |
|------|--------|------|
| `src/components/Footer.js` | ~L41 | Facebook URL |
| `src/components/Footer.js` | ~L44 | Instagram URL |
| `src/components/Footer.js` | ~L47 | LINE URL |
| `src/app/contact/page.js` | ~L49 | LINE URL |
| `src/app/contact/page.js` | ~L52 | Facebook URL |

**ตัวอย่างการแก้:** เปลี่ยน `href="#"` เป็น `href="https://facebook.com/baanpoolvilla"`

### 4. LINE OA ชื่อแสดง

| ไฟล์ | บรรทัด | ค่าปัจจุบัน | แก้เป็น |
|------|--------|-------------|---------|
| `src/components/BookingForm.js` | ~L155 | `'เพิ่มเพื่อน @TestBPV'` | ชื่อ OA จริง |

### 5. รหัส Admin (ไฟล์ .env.local)

| ตัวแปร | ค่าปัจจุบัน | ควรเปลี่ยนเป็น |
|--------|-------------|----------------|
| `ADMIN_USERNAME` | `admin` | username ที่ต้องการ |
| `ADMIN_PASSWORD` | `admin123` | รหัสที่ปลอดภัยกว่า |

### 6. URL เว็บ (ไฟล์ .env.local)

| ตัวแปร | ค่าปัจจุบัน | ควรเปลี่ยนเป็น |
|--------|-------------|----------------|
| `NEXT_PUBLIC_SITE_URL` | ngrok dev URL | `https://baanpoolvilla.vercel.app` หรือ domain จริง |

---

## 🟡 ควรแก้เพื่อ Branding

### 7. รูป Hero Banner (พื้นหลังหน้าแรก)

| ไฟล์ | บรรทัด | ค่าปัจจุบัน |
|------|--------|-------------|
| `src/components/HeroSection.js` | ~L30 | รูป Unsplash stock |

**วิธีแก้:** เปลี่ยน URL ในบรรทัด `backgroundImage:` เป็นรูปวิลล่าจริง (แนะนำ 1920px กว้าง)

### 8. รูปทำเลยอดนิยม (4 รูป)

| ไฟล์ | บรรทัด | ทำเล |
|------|--------|------|
| `src/components/PopularDestinations.js` | ~L7 | บางแสน |
| | ~L13 | พัทยา |
| | ~L19 | สัตหีบ |
| | ~L25 | ระยอง |

**วิธีแก้:** เปลี่ยน `image: 'https://images.unsplash.com/...'` เป็น URL รูปจริง

### 9. ข้อมูลทำเล — ชื่อ + จำนวนบ้าน

ไฟล์ `src/components/PopularDestinations.js`:
```js
{ name: 'บางแสน', count: 20 }  // เปลี่ยนชื่อ/จำนวนได้
{ name: 'พัทยา', count: 49 }
{ name: 'สัตหีบ', count: 11 }
{ name: 'ระยอง', count: 3 }
```

### 10. ข้อมูลสถิติ (ตัวเลข stats)

**HeroSection** (`src/components/HeroSection.js` ~L97-109):
```
'50+'          → จำนวนพูลวิลล่า
'1,000+'       → จำนวนลูกค้า
'4.8'          → คะแนนเฉลี่ย
'24/7'         → บริการลูกค้า
```

**About Page** (`src/app/about/page.js` ~L41-44):
```
'50+'   บ้านพัก
'1,000+' ลูกค้า
'5+'    ปีประสบการณ์
'4.8'   คะแนนเฉลี่ย
```

### 11. รีวิวลูกค้า (Testimonials)

ไฟล์ `src/components/Testimonials.js` (~L6-24):
```js
// รีวิวที่ 1
{ name: 'คุณสมชาย ก.', role: 'ครอบครัว 6 คน', avatar: '👨‍💼', text: '...' }
// รีวิวที่ 2
{ name: 'คุณมาลี ว.', role: 'กลุ่มเพื่อน 10 คน', avatar: '👩‍💼', text: '...' }
// รีวิวที่ 3
{ name: 'คุณวิชัย ป.', role: 'คู่รัก', avatar: '👨', text: '...' }
```

**วิธีแก้:** เปลี่ยนชื่อ, role, text เป็นรีวิวจริงจากลูกค้า

---

## 🟢 ปรับแต่ง Branding ขั้นสูง

### 12. ชื่อเว็บ / ธุรกิจ (6+ จุด)

| ไฟล์ | อะไร | ค่าปัจจุบัน |
|------|------|-------------|
| `src/app/layout.js` ~L6 | Title (แท็บ browser) | `'BaanPoolVilla - จองบ้านพักพูลวิลล่า'` |
| `src/app/layout.js` ~L7 | Meta description (SEO) | `'บริการจองบ้านพักพูลวิลล่า ราคาพิเศษ...'` |
| `src/components/Header.js` ~L51-52 | โลโก้ | `'Baan'` + `'PoolVilla'` |
| `src/components/Footer.js` ~L35 | โลโก้ footer | `'Baan'` + `'PoolVilla'` |
| `src/components/Footer.js` ~L99 | Copyright | `'© 2026 BaanPoolVilla...'` |
| `public/manifest.json` ~L2-3 | PWA name | `'BaanPoolVilla'` / `'BaanPV'` |

### 13. สีหลัก (Color Scheme — ทั้งเว็บเปลี่ยนทีเดียว)

**ไฟล์ `tailwind.config.js`** (~L11-20) — เปลี่ยน palette `primary`:
```js
primary: {
  50: '#fff7ed',    // สีอ่อนสุด
  100: '#ffedd5',
  200: '#fed7aa',
  300: '#fdba74',
  400: '#fb923c',
  500: '#f97316',   // ← สีหลัก (ปัจจุบัน: ส้ม)
  600: '#ea580c',
  700: '#c2410c',
  800: '#9a3412',
  900: '#7c2d12',
}
```

**ไฟล์ `src/app/globals.css`** (~L8-10) — เปลี่ยน CSS variables:
```css
--primary: #f97316;       /* สีหลัก */
--primary-dark: #ea580c;  /* สีหลักเข้ม */
--primary-light: #fed7aa; /* สีหลักอ่อน */
```

**ตัวอย่างเปลี่ยนเป็นสีน้ำเงิน:** แทน `#f97316` ด้วย `#3b82f6` (blue-500)

### 14. ฟอนต์

| ไฟล์ | ค่าปัจจุบัน |
|------|-------------|
| `src/app/globals.css` ~L6 | Google Fonts: `Prompt` |
| `tailwind.config.js` ~L28 | `fontFamily: { sans: ['Prompt'] }` |

เปลี่ยน `Prompt` เป็นฟอนต์ไทยอื่นๆ เช่น `Sarabun`, `Kanit`, `Noto Sans Thai`

---

## 📝 ข้อความหน้าต่างๆ

### หน้าแรก (Homepage)

| ไฟล์ | อะไร | ค่าปัจจุบัน |
|------|------|-------------|
| `src/components/HeroSection.js` ~L38-39 | หัวข้อใหญ่ | `'ค้นหาพูลวิลล่า สำหรับวันพักผ่อนของคุณ'` |
| `src/components/HeroSection.js` ~L42 | คำอธิบายย่อย | `'บ้านพักพูลวิลล่าส่วนตัว พร้อมสระว่ายน้ำ...'` |
| `src/app/page.js` ~L26 | CTA heading | `'พร้อมออกเดินทางหรือยัง?'` |
| `src/app/page.js` ~L28 | CTA subtext | `'จองพูลวิลล่าของคุณวันนี้...'` |
| `src/app/FeaturedVillas.js` ~L52-53 | Section title | `'พูลวิลล่าแนะนำ'` / `'ที่พักยอดนิยม...'` |
| `src/components/Features.js` ~L5-18 | 4 features | ราคาดีที่สุด, จองง่าย, 24/7, คุณภาพ |

### หน้าเกี่ยวกับเรา (About)

| ไฟล์ | อะไร |
|------|------|
| `src/app/about/page.js` ~L23 | หัวข้อ: `'ยินดีต้อนรับสู่ BaanPoolVilla'` |
| ~L25-35 | 3 ย่อหน้าอธิบายธุรกิจ (เปลี่ยนเนื้อหาได้เลย) |

### หน้ารายการวิลล่า (Villas Listing)

| ไฟล์ | อะไร |
|------|------|
| `src/app/villas/page.js` ~L75-76 | Banner: `'รวมพูลวิลล่าทั้งหมด'` + `'ค้นหาพูลวิลล่าที่ตอบโจทย์คุณ'` |

### หน้าติดต่อ (Contact)

| ไฟล์ | อะไร |
|------|------|
| `src/app/contact/page.js` ~L27-28 | Title + subtitle |
| ~L39 | เวลาทำการ: `'จันทร์ - อาทิตย์ 9:00 - 21:00'` |
| ~L45 | เวลาตอบอีเมล: `'ตอบกลับภายใน 24 ชั่วโมง'` |

---

## 🧭 Menu & Navigation

ไฟล์ `src/components/Header.js` (~L30-33):
```js
{ name: 'หน้าแรก', href: '/' },
{ name: 'พูลวิลล่าทั้งหมด', href: '/villas' },
{ name: 'เกี่ยวกับเรา', href: '/about' },
{ name: 'ติดต่อเรา', href: '/contact' },
```

**เปลี่ยนชื่อเมนู:** แก้ `name` ได้เลย  
**เพิ่ม/ลบเมนู:** เพิ่ม/ลบ object ในอาร์เรย์

---

## 🔧 Zone / ทำเล (Dropdown + Filter)

ปรากฏใน 2 ที่:

**1. Hero search dropdown** (`src/components/HeroSection.js` ~L58-63):
```html
<option value="">ทำเลทั้งหมด</option>
<option value="pattaya">พัทยา-ชลบุรี</option>
<option value="huahin">หัวหิน-ประจวบฯ</option>
...
```

**2. Villa detail** (`src/app/villas/[id]/page.js` ~L80-85):
```js
const zoneLabels = {
  pattaya: 'พัทยา-ชลบุรี',
  huahin: 'หัวหิน-ประจวบฯ',
  khaoyai: 'เขาใหญ่-นครราชสีมา',
  rayong: 'ระยอง',
  bangkok: 'กรุงเทพฯ-ปริมณฑล',
};
```

**เพิ่มทำเลใหม่:** เพิ่ม `<option>` ใน Hero + เพิ่มใน `zoneLabels` object

---

## 💡 วิธีแก้ไข

1. เปิดไฟล์ที่ต้องการใน VS Code
2. ค้นหาข้อความเดิม (Ctrl+F)
3. เปลี่ยนเป็นข้อความใหม่
4. บันทึก (Ctrl+S)
5. Push ขึ้น GitHub:
   ```bash
   cd website
   git add .
   git commit -m "อัปเดตข้อมูล"
   git push
   ```
6. Vercel จะ deploy อัตโนมัติภายใน ~1 นาที

---

*อัปเดตล่าสุด: 9 ก.พ. 2569*
